export interface ListarContatoViewModel {
  id: string;
  nome: string;
  email: string;
  telefone: string;
  empresa: string;
  cargo: string;
}
export interface CadastrarContatoViewModel {
  nome: string;
  email: string;
  telefone: string;
  empresa: string;
  cargo: string;
}
export interface EditarContatoViewModel {
  nome: string;
  email: string;
  telefone: string;
  empresa: string;
  cargo: string;
}
export interface ExcluirContatoViewModel{}
export interface VizualiarContatoViewModel {
  id: string;
  nome: string;
  email: string;
  telefone: string;
  empresa: string;
  cargo: string;
}
export interface ListarContatoViewModel {
  id: string;
  nome: string;
  email: string;
  telefone: string;
  empresa: string;
  cargo: string;
}
